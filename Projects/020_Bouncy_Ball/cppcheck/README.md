### `exclude_files`
Specific files to skip.

Use this when:
- one file is third-party
- one file is temporary noisy
- one file should be ignored for now

Example:

```json
"exclude_files": [
    "Core/drivers/display/gc9a01a.c"
]